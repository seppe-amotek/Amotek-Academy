const academySlider = new Swiper('.swiper.is-academy', {
  // Optional parameters
  loop: true,
  sliderPerView: 4,
  },
                                 
  // And if we need scrollbar
  scrollbar: {
    el: '.swiper-scrollbar',
  },
});
